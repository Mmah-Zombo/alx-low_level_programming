#include "main.h"
#include <string.h>

/**
 * *_strcat - start here
 * @dest: value one
 * @src: value two
 *
 * Return: a pointer to the result string dest
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
