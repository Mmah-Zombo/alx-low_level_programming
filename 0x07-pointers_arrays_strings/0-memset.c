#include "main.h"
#include <string.h>

/**
 * *_memset - start here
 * @s: i don't know yet
 * @b: idk
 * @n: idk
 *
 * Return: a pointer to the memory area s
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s, b, n));
}
