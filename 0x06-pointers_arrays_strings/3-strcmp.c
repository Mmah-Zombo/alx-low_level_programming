#include "main.h"
#include <string.h>

/**
 * _strcmp - the name of the function
 * @s1: value one
 * @s2: value two
 *
 * Return: 0 or a positive number or a negative number
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
